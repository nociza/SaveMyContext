/** Public embedding contract for the framework-independent web component. */
export function resolveApiBase(value?: string, pageUrl?: string): string;
export class SMCWorkspace extends HTMLElement {
  token: string;
  load(): Promise<void>;
}
